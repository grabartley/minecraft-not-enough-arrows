package com.grahambartley.morearrows;

import com.terraformersmc.modmenu.api.ModMenuApi;

/**
 * Loaded by Mod Menu through the {@code modmenu} entrypoint in {@code fabric.mod.json}. Nothing
 * else references this class, so it stays unloaded when Mod Menu is not installed.
 *
 * <p>No configuration screen factory is supplied yet, so Mod Menu shows the mod without a configure
 * button.
 */
public final class MoreArrowsModMenu implements ModMenuApi {}
