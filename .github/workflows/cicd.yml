name: CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump"
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
        default: patch
      release_type:
        description: "Release type"
        required: true
        type: choice
        options:
          - alpha
          - beta
          - stable
        default: alpha

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      checks: write
      pull-requests: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}

      - name: Check formatting (Spotless)
        run: ./gradlew spotlessCheck --configuration-cache --build-cache

      - name: Run tests with coverage
        run: ./gradlew check --configuration-cache --build-cache

      - name: Run gametests
        run: ./gradlew runGametest --no-daemon --stacktrace

      - name: Dedicated server smoke boot
        run: timeout 120s ./gradlew runServer --no-daemon --stacktrace --args="nogui" || test $? -eq 124

      - name: Publish unit test report
        uses: mikepenz/action-junit-report@v4
        if: success() || failure()
        with:
          report_paths: "**/build/test-results/*/TEST-*.xml"
          detailed_summary: true
          include_passed: true

      - name: Upload gametest run dir on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: gametest-run-${{ github.sha }}
          path: build/gametest/

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: success() || failure()
        with:
          name: coverage-report
          path: build/reports/jacoco/test/html/

      # ---------- Non-release builds (PR + push to main) ----------
      - name: Build snapshot JAR
        if: github.event_name == 'pull_request' || github.event_name == 'push'
        run: ./gradlew build -Pversion=1.0.0-SNAPSHOT-${{ github.sha }}

      - name: Upload snapshot JAR
        if: github.event_name == 'pull_request' || github.event_name == 'push'
        uses: actions/upload-artifact@v4
        with:
          name: not-enough-arrows-${{ github.sha }}
          path: build/libs/*.jar

      # ---------- Release-only (manual trigger) ----------
      - name: Compute next version
        if: github.event_name == 'workflow_dispatch'
        id: semver
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          PREV_RELEASE=$(gh release list --limit 100 --json tagName,publishedAt \
            --jq 'map(select(.publishedAt != null))
                  | sort_by(.publishedAt)
                  | reverse
                  | .[0] // {tagName: "v0.0.0", publishedAt: ""}
                  | "\(.tagName)\t\(.publishedAt)"')

          PREV_TAG=$(cut -f1 <<< "$PREV_RELEASE")
          PREV_PUBLISHED=$(cut -f2 <<< "$PREV_RELEASE")
          PREV_VERSION=${PREV_TAG#v}

          if [[ ! "$PREV_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Could not parse a semver version from previous release tag '${PREV_TAG}'"
            exit 1
          fi

          IFS='.' read -r MAJOR MINOR PATCH <<< "$PREV_VERSION"
          case "${{ inputs.bump }}" in
            major) MAJOR=$((MAJOR + 1)); MINOR=0; PATCH=0 ;;
            minor) MINOR=$((MINOR + 1)); PATCH=0 ;;
            patch) PATCH=$((PATCH + 1)) ;;
          esac
          NEW_VERSION="${MAJOR}.${MINOR}.${PATCH}"

          echo "Previous: ${PREV_VERSION} (published ${PREV_PUBLISHED:-never})"
          echo "New:      ${NEW_VERSION}"

          {
            echo "new_version=${NEW_VERSION}"
            echo "prev_tag=${PREV_TAG}"
            echo "prev_published=${PREV_PUBLISHED}"
          } >> "$GITHUB_OUTPUT"

      - name: Map release type to Modrinth version type
        if: github.event_name == 'workflow_dispatch'
        id: release_meta
        run: |
          case "${{ inputs.release_type }}" in
            stable) echo "modrinth_type=release" >> $GITHUB_OUTPUT ;;
            beta)   echo "modrinth_type=beta"    >> $GITHUB_OUTPUT ;;
            alpha)  echo "modrinth_type=alpha"   >> $GITHUB_OUTPUT ;;
          esac

      - name: Build release JAR
        if: github.event_name == 'workflow_dispatch'
        run: ./gradlew build -Pversion=${{ steps.semver.outputs.new_version }}

      - name: Generate changelog
        if: github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api \
            --method POST \
            /repos/${{ github.repository }}/releases/generate-notes \
            -f tag_name=v${{ steps.semver.outputs.new_version }} \
            --jq .body > release-notes.md

          if [ ! -s release-notes.md ]; then
            echo "::warning::Generated changelog is empty"
            echo "No changes recorded for this release." > release-notes.md
          fi

          echo "--- Changelog ---"
          cat release-notes.md

      - name: Create GitHub tag
        if: github.event_name == 'workflow_dispatch'
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          custom_tag: ${{ steps.semver.outputs.new_version }}
          tag_prefix: v

      - name: Create GitHub release
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.semver.outputs.new_version }}
          files: build/libs/*.jar
          body_path: release-notes.md
          prerelease: ${{ inputs.release_type != 'stable' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to Modrinth
        if: github.event_name == 'workflow_dispatch'
        run: ./gradlew modrinth -Pversion=${{ steps.semver.outputs.new_version }}
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
          MOD_RELEASE_TYPE: ${{ steps.release_meta.outputs.modrinth_type }}

      # Runs after publishing and never fails the build: the artifacts are already live,
      # so a bookkeeping problem must not red-X a shipped release.
      - name: Create and apply release milestone
        if: github.event_name == 'workflow_dispatch'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MILESTONE: v${{ steps.semver.outputs.new_version }}
          PREV_PUBLISHED: ${{ steps.semver.outputs.prev_published }}
        run: |
          set -uo pipefail

          if [ -z "$PREV_PUBLISHED" ]; then
            echo "No previous release; treating every merged pull request as part of ${MILESTONE}."
            PREV_PUBLISHED="1970-01-01T00:00:00Z"
          fi

          ISSUES=$(gh pr list --state merged --base main --limit 300 \
            --json mergedAt,closingIssuesReferences \
            --jq "[.[]
                   | select(.mergedAt != null and .mergedAt > \"${PREV_PUBLISHED}\")
                   | .closingIssuesReferences[].number]
                  | unique | .[]")

          if [ -z "$ISSUES" ]; then
            echo "::warning::No closed issues found since ${PREV_PUBLISHED}; skipping ${MILESTONE} milestone."
            exit 0
          fi

          NUMBER=$(gh api "repos/${GITHUB_REPOSITORY}/milestones?state=all&per_page=100" \
            --jq "map(select(.title == \"${MILESTONE}\")) | .[0].number // empty")

          if [ -z "$NUMBER" ]; then
            NUMBER=$(gh api -X POST "repos/${GITHUB_REPOSITORY}/milestones" \
              -f title="${MILESTONE}" \
              -f description="Shipped in ${MILESTONE}." \
              --jq .number)
            echo "Created milestone ${MILESTONE} (#${NUMBER})."
          else
            echo "Reusing existing milestone ${MILESTONE} (#${NUMBER})."
          fi

          for issue in $ISSUES; do
            if gh issue edit "$issue" --milestone "${MILESTONE}" >/dev/null 2>&1; then
              echo "  #${issue} -> ${MILESTONE}"
            else
              echo "::warning::Could not set ${MILESTONE} on issue #${issue}"
            fi
          done

          gh api -X PATCH "repos/${GITHUB_REPOSITORY}/milestones/${NUMBER}" \
            -f state=closed --jq '"Closed milestone \(.title)."'